# 1.0 PRD — A/B/C 分类决策逻辑重构

> 需求来源：一个结构上明显属于 C 的音频被判为 B。
> 目标不是修复这一个文件，而是整体修复分类决策逻辑中已确认的规则冲突。
> 上下文入口：先读 `project.md`，再读本文件。

## 1. 问题与证据

触发样本：`/Users/xy/Downloads/1784269734_123857054037_codec_out_8a43de53_0.wav`

当前分类器实测输出（已复现）：

```text
LABEL: template_B
template_C  hits=2  qualified=True   strong=['mega_lowmid', 'extreme_body_to_presence']
template_B  hits=1  qualified=False  strong=['very_spiky_harsh']
template_A  hits=0  qualified=False  strong=[]

lowmid=0.7205  upper=0.0127  harsh=0.0256
group_ratios: body=0.9233  presence=0.0383
body_to_presence=24.08
peakiness_upper=8.39   peakiness_harsh=12.58
```

这是结构上典型的 C：主体占 92%，presence 坍塌到 3.8%，body/presence 达 24。
它输给了一个**未达标**的 B（1 个普通命中，`minimum_hits=2`），唯一原因是
`peakiness_harsh` 比 `DECISIVE_HARSH_PEAK_DB` 高了 0.58 dB，触发硬覆盖。

## 2. 已确认的规则冲突

以下九项均已对照代码和真实样本验证，不是推测：

1. **B 的硬覆盖绕过 `minimum_hits`**：`peakiness_harsh >= 12` 直接返回 B，不检查任何其他证据。
2. **普通排序路径同样不检查 `qualified`**：构造「单个未达标 B 命中、无强命中」的输入，
   排序路径仍返回 B。`qualified` 在 `classify` 中计算后从未被读取，是纯装饰字段。
3. **A/B/C 规则数量不同**：A 有 3 条普通规则、B 有 5 条、C 有 4 条。直接比较 `hits`
   对规则少的模板不公平。
4. **同一指标重复计票**：例如 B 的 `harsh_ratio_high` 与 `very_high_harsh_ratio`、
   C 的 `extreme_lowmid` 与 `mega_lowmid` 来自同一指标，会被重复统计。
5. **`peakiness_upper` 语义冲突**：同时支撑 B 的 `upper_peak_spiky`（判为"刺"）和
   C 的 `body_peak_spiky`（判为"主体峰"），语义相反。
6. **平票固定 A > B > C**：`-candidates.index(t)` 是实现顺序，没有声学依据。
7. **静默回落 A 不可见**：无证据输入返回 `template_A` 且 `qualified=False`，
   输出中没有任何 fallback 或置信度字段，下游无法区分"确信是 A"和"什么都没测到"。
8. **`flatness_upper` 未接入决策**：已计算并输出，但 `classify` 不读取。
9. **planner 只消费主模板**：`plan_mix_template.py` 的 `classification_hits()` 会展开
   所有模板的命中并标记 `selected_template_rule`，非主模板命中在后续处理中被忽略。

## 3. 设计原则

1. **不引入加权评分。** 当前只有 8 个线上回归样本（A×6、B×1、C×1），
   不足以支撑一套可信权重；拟合出的权重只会过拟合这 8 个样本。
   本次采用「证据组 + 明确优先级」的分层规则。
2. **不修改 `music_auto_mix1`。** 该仓库只从分析 JSON 读取 `classification.label`
   （`select_template` 是字典查表）。本次改动全部落在本仓库，
   `music_auto_mix1` 生产代码改动 0 行，其 plan hash / DSP / 渲染顺序 / SHA256 回归
   在结构上不可能受影响。
3. **基线先行。** 规则改动必须对照 `tests/baseline.json` 的实测 diff 说明，
   不接受"预计不变"这类估计。

## 4. 需求

### 4.1 分层决策逻辑

用「主模板 + 次要问题保护」替换当前的硬覆盖 + 平级排序：

1. **C 优先**：C 代表严重 body-heavy / presence-starved 结构。达标时优先作为主模板。
2. **A 次之**：A 代表未进入 C 结构区域的中度 muddy / boxy。
3. **B 必须有独立高频证据**，不能由单个尖峰成立。满足以下任一：
   - 高频能量异常 + 高频峰形异常；或
   - 两个高频能量频段同时异常；或
   - `upper` 与 `harsh` 两处都尖，且 `harsh` 达到强等级。
4. **B 与 A 同时成立**：只有「强 B」覆盖 A；普通 B 作为次要问题交给通用
   de-esser / HF guard。
5. **B 与 C 同时成立**：默认选 C。只有多证据的强 B 才允许竞争；
   **单一 harsh 峰永远不能推翻已达标的 C**。
6. **只比较 qualified 模板**：无模板达标时仍回落 A，但必须记录
   `fallback=true`、原因和低置信度。
7. **不再按原始 hits 数量决定模板**：改为证据组 + 明确优先级，消除重复计票
   和规则数量不对等的问题。

### 4.2 次要问题处理

次要问题只进入已有的动态齿音 / HF 保护，不重复叠加固定 EQ，避免过度削高频。

### 4.3 `flatness_upper` 处理

**明确标注为诊断特征，本次不接入分类决策。**

理由：它针对的是「AI 分离电音 / 金属环 vs 普通闷糊人声」的区分（peakiness 和
频段占比对这两类几乎无差别），是真实且未被其他指标覆盖的盲区。但目前没有
标注数据可校准阈值。**保留该指标及其实现，不得以"清理悬空代码"为由删除**；
待有标注样本后再作为分类输入评估。

### 4.4 输出契约变更

`classification` 新增：

- `fallback`：布尔，是否为无证据回落。
- `fallback_reason`：回落原因。
- `confidence`：置信度等级。
- `secondary_issues`：非主模板但成立的问题，供下游 de-esser / HF guard 使用。

现有字段（`label`、`label_name`、`template_A/B/C` 及其 `hits`/`hit_rules`/
`strong_rules`/`qualified`）保持不变，避免破坏 `batch_analyze_spectrum.py`
和 `tests/capture_baseline.py`。

## 5. 回归基线

`tests/baseline.json` 已捕获，且经过验证：用当前分类器重跑真实音频，
与 `music_auto_mix1` 存档的生产 label 对比 **8/8 完全一致**。

```text
row2  template_A      row6  template_B
row3  template_C      row7  template_A
row4  template_A      row8  template_A
row5  template_A      row9  template_A
badcase_codec_out_8a43de53  template_B   ← 预期唯一变化
```

样本分布高度偏斜（A×6、B×1、C×1）。**row3 是唯一的 C，row6 是唯一的 B**，
它们是仅有的能反映 B/C 规则变化的样本，改动时需重点审阅。

（注：`project.md` 提到的线上样本为 row2–row10，但 row1/row10 没有留下
`analysis.json`，实际可验证的是 8 个。）

## 6. 验收标准

1. **badcase 翻转**：`badcase_codec_out_8a43de53` 的 label 从 `template_B` 变为
   `template_C`。
2. **其余样本不变**：row2–row9 的 label 全部保持不变，由
   `python3 tests/capture_baseline.py --check` 实测证明，不接受估计。
3. **`qualified` 成为真实门槛**：未达标模板不能成为最终 label（回落路径除外）。
4. **单一 harsh 峰不能推翻已达标 C**：需有针对性测试覆盖。
5. **回落可见**：无证据输入的输出中 `fallback=true` 且带原因和低置信度。
6. **新增测试覆盖**：
   - 规则边界矩阵（各阈值上下边界）；
   - 复合问题（B+C 同时成立、B+A 同时成立）；
   - 无命中输入；
   - 低采样率输入；
   - 本次 badcase 必须选 C 的回归。
7. **可移植测试**：新增测试不依赖本机绝对路径（当前基线依赖 `/Users/xy/Downloads`
   和另一仓库的 `run_outputs`，缺样本会静默跳过）。
8. **`music_auto_mix1` 改动 0 行**，其既有 baseline 继续通过。
9. **文档同步**：`README.md` 的「模板规则」「决策流程」章节和 `project.md` 的
   §6.4、§7.4、§8 必须与新逻辑一致。
10. Python 编译检查通过。

## 7. 明确不做

1. 不引入加权评分或机器学习模型。
2. 不修改频段定义、`BANDS` 边界、peakiness / flatness 的数学实现。
3. 不修改 `music_auto_mix1` 的模板选择、DSP、渲染顺序、loudness 或 guards。
4. 不删除 `flatness_upper`。
5. 不为了消除基线 diff 而直接覆盖 `baseline.json`；只有确认变化符合本 PRD
   预期后才能重新 capture。
